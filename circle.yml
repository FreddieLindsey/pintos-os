test:
  override:
    - make -C src/devices

deployment:
  production:
    branch: master
    commands:
      - git remote add gitlab git@gitlab.doc.ic.ac.uk:lab1516_spring/pintos_12.git
      - git push gitlab master
